language: node_js

node_js:
- 12.13.0
cache: npm

branches:
  only:
  - master
  - /v(\d)\.(\d)\.(\d)(\-\d)?/

deploy:
    provider: pages
    script: 
      - npm ci
      - npm run build-storybook 
    skip-cleanup: true
    github-token: $GITHUB_TOKEN
    on:
      tags: true